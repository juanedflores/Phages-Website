function setup () {
  createCanvas(windowWidth, windowHeight);
  background(0);
}

function draw () {
  stroke(255, 0, 0);
  strokeWeight(5);
  textSize(120);
  text("Phages", windowWidth / 2, windowHeight / 2);
}
